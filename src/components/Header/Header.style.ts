import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledHeaderActionsContainer = styled(Box)`
    display: flex;
    align-items: center;
    gap: 8px;
`;

export const StyledLogoMark = styled(Box)`
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: inherit;
`;
